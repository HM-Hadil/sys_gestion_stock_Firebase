export interface Etudiant {
    $key?: string; // Utilisé pour les clés Firebase
    id?: string;
    
    Cin?: string;
    firstName?: string;
    lastName?: string;
    email?: string;
    phone?: string;
  }
  